server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: lamp-back
  datasource:
    url: jdbc:mysql://localhost:3306/lamp?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

# 无 MySQL 时使用 H2 内存库（开发可切换）
#spring:
#  datasource:
#    url: jdbc:h2:mem:lamp;DB_CLOSE_DELAY=-1
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect

jwt:
  secret: lamp-graduate-attendance-lab-booking-secret-key-2024
  expiration-ms: 86400000
